[project]
name = "fe-setup-mcp-server"
version = "0.1.0"
description = "MCP server for FE environment setup, repo cloning, and terminal management"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0",
    "pyyaml",
]

[project.scripts]
fe-setup-mcp = "server_fe_setup:main"
