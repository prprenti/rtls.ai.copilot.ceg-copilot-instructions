[project]
name = "turnin-mcp-server"
version = "0.1.0"
description = "MCP server for turnin submission and gatekeeper log access"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0",
]

[project.scripts]
turnin-mcp = "server_turnin:main"
