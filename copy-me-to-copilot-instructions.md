# Copilot Instructions for Intel Design Repository

## Repository Setup

To set up this repository environment, invoke the `fe-setup` agent (`@fe-setup`). It runs `cth_psetup`, clones repos from `ceg_repos.yml`, sets `WORKAREA`, and manages terminal sessions.

## Branch

The default branch is `ttl-a0`.

